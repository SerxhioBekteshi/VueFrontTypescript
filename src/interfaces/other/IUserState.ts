export interface IUserState {
  email: string;
  id: number;
  role: string;
  name: string;
  lastName: string;
  nipt: string;
  accountSubmitted: boolean;
  quizFulfilled: boolean;
  photo: string;
}
